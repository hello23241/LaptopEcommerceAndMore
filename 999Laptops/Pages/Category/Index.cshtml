@model List<LaptopEcommerceAndMore.Models.Categories>

@{
    ViewData["Title"] = "Categories";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var userName = Context.Session.GetString("UserName");
    var isAdmin = userName == "admin";
}

<div class="container mt-4">
    <h1 class="mb-4">Categories</h1>

    @if (isAdmin)
    {
        <a href="@Url.Action("Create")" class="btn btn-primary mb-3">Add New Category</a>
    }

    @if (Model.Count > 0)
    {
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr>
                        <td>@category.CategoryId</td>
                        <td>@category.CategoryName</td>
                        <td>@category.Description</td>
                        <td>
                            <a href="@Url.Action("Details", new { id = category.CategoryId })" class="btn btn-sm btn-info">View</a>
                            <a href="@Url.Action("Edit", new { id = category.CategoryId })" class="btn btn-sm btn-warning">Edit</a>
                            <a href="@Url.Action("Delete", new { id = category.CategoryId })" class="btn btn-sm btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info">No categories found. <a href="@Url.Action("Create")">Create one</a></div>
    }
</div>

