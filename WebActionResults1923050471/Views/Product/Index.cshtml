@model List<WebActionResults1923050471.Models.Product>

@{
    ViewData["Title"] = "Products";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var userId = Context.Session.GetInt32("UserId");
    var userName = Context.Session.GetString("UserName");
    var isAdmin = userName == "admin";
}

<div class="container mt-4">
    <h1 class="mb-4">Products</h1>

    @if (isAdmin)
    {
        <a href="@Url.Action("Create")" class="btn btn-primary mb-3">Add New Product</a>
    }

    @if (Model.Count > 0)
    {
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Quantity</th>
                    <th>Category</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var product in Model)
                {
                    <tr>
                        <td>@product.Id</td>
                        <td>@product.Name</td>
                        <td>$@product.Price.ToString("F2")</td>
                        <td>@product.Quantity</td>
                        <td>@product.CategoryId</td>
                        <td>
                            <a href="@Url.Action("Details", new { id = product.Id })" class="btn btn-sm btn-info">View</a>
                            <a href="@Url.Action("Edit", new { id = product.Id })" class="btn btn-sm btn-warning">Edit</a>
                            <a href="@Url.Action("Delete", new { id = product.Id })" class="btn btn-sm btn-danger">Delete</a>
                            @if (userId.HasValue)
                            {
                                <form asp-controller="Cart" asp-action="AddToCart" method="post" style="display: inline;">
                                    <input type="hidden" name="productId" value="@product.Id" />
                                    <input type="hidden" name="quantity" value="1" />
                                    <button type="submit" class="btn btn-sm btn-success">Add to Cart</button>
                                </form>
                            }
                            else
                            {
                                <a href="@Url.Action("Login", "Account")" class="btn btn-sm btn-outline-success">Add to Cart</a>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info">No products found. <a href="@Url.Action("Create")">Create one</a></div>
    }
</div>
