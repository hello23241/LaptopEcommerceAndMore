@model WebActionResults1923050471.Models.ProductDetailsViewModel

@{
    ViewData["Title"] = "Product Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h1>@Model.Product.ProductName</h1>
    
    <div class="card">
        <div class="card-body">
            <p><strong>ID:</strong> @Model.Product.ProductID</p>
            <p><strong>Price:</strong> $@Model.Product.BasePrice.ToString("F2")</p>
            <p><strong>Quantity:</strong> @Model.Product.StockQuantity</p>
            <p><strong>Category ID:</strong> @Model.Product.CategoryID</p>
            <p><strong>Brand ID:</strong> @Model.Product.BrandID</p>
            <div class="mt-3">
                @await Component.InvokeAsync("CompareButton", new { productId = Model.Product.ProductID })
            </div>
        </div>
    </div>

    <div class="card mt-4">
        <div class="card-body">
            <ul class="nav nav-tabs" role="tablist">
                @for (var index = 0; index < Model.Tabs.Count; index++)
                {
                    var tab = Model.Tabs[index];
                    var isActive = index == 0;
                    <li class="nav-item" role="presentation">
                        <button class="nav-link @(isActive ? "active" : "")" id="tab-@index" data-bs-toggle="tab" data-bs-target="#tab-content-@index" type="button" role="tab" aria-controls="tab-content-@index" aria-selected="@(isActive ? "true" : "false")">@tab.Title</button>
                    </li>
                }
            </ul>
            <div class="tab-content pt-3">
                @for (var index = 0; index < Model.Tabs.Count; index++)
                {
                    var tab = Model.Tabs[index];
                    var isActive = index == 0;
                    <div class="tab-pane fade @(isActive ? "show active" : "")" id="tab-content-@index" role="tabpanel" aria-labelledby="tab-@index">
                        @foreach (var line in tab.Content.Split('\n', StringSplitOptions.RemoveEmptyEntries))
                        {
                            <p>@line</p>
                        }
                    </div>
                }
            </div>
        </div>
    </div>

    <a href="@Url.Action("Edit", new { id = Model.Product.ProductID })" class="btn btn-warning mt-3">Edit</a>
    <a href="@Url.Action("Index")" class="btn btn-secondary mt-3">Back to List</a>
</div>
