@model List<WebActionResults1923050471.Models.Account>

@{
    ViewData["Title"] = "Accounts";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <h1 class="mb-4">User Accounts</h1>
    
    <a href="@Url.Action("Create")" class="btn btn-primary mb-3">Add New Account</a>

    @if (Model.Count > 0)
    {
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Full Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var account in Model)
                {
                    <tr>
                        <td>@account.Id</td>
                        <td>@account.UserName</td>
                        <td>@account.FullName</td>
                        <td>@account.Email</td>
                        <td>@account.Phone</td>
                        <td>
                            @if (account.Status == 1)
                            {
                                <span class="badge bg-success">Active</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">Inactive</span>
                            }
                        </td>
                        <td>
                            <a href="@Url.Action("Details", new { id = account.Id })" class="btn btn-sm btn-info">View</a>
                            <a href="@Url.Action("Edit", new { id = account.Id })" class="btn btn-sm btn-warning">Edit</a>
                            <a href="@Url.Action("Delete", new { id = account.Id })" class="btn btn-sm btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-info">No accounts found.</div>
    }
</div>
